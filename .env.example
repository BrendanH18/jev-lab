# Copy this file to .env (which is gitignored), or let the app write .env for you from the
# "Connect API key" dialog. These names match the official TypeSafe SDKs.

TYPESAFE_API_KEY=

# Optional
# TYPESAFE_DEFAULT_MODEL=jev-latest
# TYPESAFE_BASE_URL=https://api.typesafe.ai
# JEV_LAB_BUDGET_USD=2.00      # stop live calls after this much spend in one server run
# JEV_LAB_RPM=120              # local cap on live calls per minute
# PORT=8321
